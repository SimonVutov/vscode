{
  "name": "sample-text-panel",
  "displayName": "Sample Text Panel",
  "description": "Shows a sample text panel when a code file is selected",
  "version": "1.0.0",
  "publisher": "vscode",
  "engines": {
    "vscode": "^1.70.0"
  },
  "main": "./out/extension.js",
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "sampleTextPanel.toggle",
        "title": "Toggle Sample Text Panel"
      }
    ],
    "configuration": {
      "title": "Sample Text Panel",
      "properties": {
        "sampleTextPanel.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable/disable automatic sample text panel when code files are opened"
        },
        "sampleTextPanel.sampleText": {
          "type": "string",
          "default": "// This is a sample text panel\n// It appears when you select a code file\n// You can customize this text in settings\n\nconst sampleFunction = () => {\n  console.log('Hello from sample text!');\n};\n\n// Some useful code snippets:\n// 1. Console logging\n// 2. Function declarations\n// 3. Variable assignments\n// 4. Comments and documentation",
          "description": "The sample text to display in the panel",
          "editPresentation": "multilineText"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.70.0",
    "typescript": "^4.9.4"
  }
}
